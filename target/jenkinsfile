pipeline {
parameters {
  string defaultValue: 'master', name: 'branch', trim: true
}

     agent any
     stages {
         stage('build') {
              when {
                  branch 'master'             
              }
              steps {
                 echo "Working on master branch"
              }
         }
     }
}
